<% if signed_in? && current_user.admin?%>
  <%= render "categories/form", target: @category %>
<% end %>

<h1><%= @area.name %></h1>

<h3>Categories</h3>

<ul>
  <% @area.categories.each do |category| %>
    <li>
      <%= category.name %>
    </li>
  <% end %>
</ul>
