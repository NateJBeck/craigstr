<% if signed_in? && current_user.admin? %>
  <%= render "categories/form", target: @category %>
<% end %>

<% if signed_in? %>
  <%= render "posts/form", target: @post %>
<% end %>

<h1><%= @area.name %></h1>

<h3>Categories</h3>

<ul>
  <%= render @categories %>
</ul>

<%= render @posts %>
